# Default Shell
set-option -g default-shell /usr/local/bin/bash

# Bindings
bind-key C-b last-window
bind k command-prompt "kill-window %%"

# Theme
set -g status-bg blue
set -g status-fg white
set -g status-right '#(date -u +%%H:%%M) UTC #[default]'
